app RedButton initial state off {
	bricks
		Actuator buzzer : 12
		Sensor b1 : 8
		Sensor b2 : 9
	
	states
		off {
			buzzer <= LOW
			AND {
				b1 is HIGH
				b2 is HIGH
			} => on
		}
		on {
			buzzer <= HIGH
			OR {
				b1 is LOW
				b2 is LOW
			} => off
		}
}

