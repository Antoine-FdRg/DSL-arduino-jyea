app RedButton initial state off {
	bricks
		Actuator red_led : 12
		Sensor b1 : 8
		Sensor b2 : 9
	
	states
		off {
			red_led <= LOW
			AND {
				b1 is HIGH
				b2 is HIGH
			} => on
		}
		on {
			red_led <= HIGH
			OR {
				b1 is LOW
				b2 is LOW
			} => off
		}
}

